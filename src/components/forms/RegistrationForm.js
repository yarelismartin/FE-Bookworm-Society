import React from 'react';

export default function RegistrationForm() {
  // 1. Need default inputs
  // 2. for update we need to accept a userObj in the parameters of the form component
  // 3. need a state for form input
  // 4. handle change function to capture the users input
  // target the name and value PAIRS
  // refrence your state and grab the prevstate so your include it alsong with the name and value pair your targeted
  // 5. handle submit so that we can call our create and update endpoints
  // prevent default ????
  // 6. useEffect so that if the userObj.id == true then we showcase the inputs
  // 7. my form (JSON)
  // you will nee a name, and b=value
  // 8. Need proptypes section

  return (
    <div>
      <h1>hey its time to register</h1>
    </div>
  );
}
